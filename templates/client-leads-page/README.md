# תבנית: דף לידים ייעודי ללקוח

נקודת התחלה לבניית דף לידים נפרד (גיליון + Apps Script) ללקוח חדש, מבוססת על הפרויקט שנבנה לארז קדם אקדמיה (`../erez-kedem-academy-leads/`).

**קודם לקרוא את `BUILD-GUIDE.md`** — הוא כולל את השאלות לשאול לפני שמתחילים, את כל ההחלטות שכבר התקבלו (ולמה), ואת כל התקלות שכבר נתקלנו בהן בפעם הראשונה (ואיך למנוע אותן מראש).

אחרי שהאפיון סגור: להעתיק את חמשת הקבצים כאן לתיקייה חדשה בשם הלקוח (למשל `<שם-לקוח>-leads/`), ולעבור על כל מופע של `TODO(client)` בקוד ולמלא לפי הלקוח הספציפי.
